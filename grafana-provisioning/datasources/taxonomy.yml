apiVersion: 1
datasources:
  - name: Alerts Taxonomy API
    type: marcusolsson-json-datasource
    access: proxy
    url: http://localhost:8000
    isDefault: false
    jsonData:
      tlsSkipVerify: true
      cacheDurationSeconds: 30
    editable: true
    # Use /taxonomy/alerts endpoint for table; panel query should point there.
